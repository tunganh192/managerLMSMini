spring:
  application:
    name: managerLMSMini

  datasource:
    url: jdbc:mariadb://210.211.101.27:5019/TungAnhTest?useUnicode=true&characterEncoding=utf8
    username: root
    password: quiPIgINaBLe
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: Asia/Bangkok

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB

app:
  upload-dir: uploads

logging:
  level:
    org.hibernate.SQL: DEBUG
